{
  "name": "trasuahem",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate": "node scripts/validate-content.mjs",
    "check": "npm run validate && npm run build"
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "lucide": "^1.39.0"
  }
}
